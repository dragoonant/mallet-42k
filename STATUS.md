# Status

Maintained by agents as work lands. Orient from this file instead of exploring the codebase.

## What exists (M0 — project scaffold)

| Area | State |
|---|---|
| Build | Vite + React + TypeScript (strict), package `mallet-42k`, ESM (`"type": "module"`) |
| Deps | three, @react-three/fiber, @react-three/drei, zustand; dev: typescript, vite, @vitejs/plugin-react, vitest, @playwright/test, @types/* |
| Config | `vite.config.ts` (base `/mallet-42k/` in prod, `/` in dev; `@` → `src`; vitest env `node`, `tests/**/*.test.ts`); `tsconfig.json` (strict, includes `src` + `tests`, works with or without `src/engine`) |
| Client | `src/main.tsx`, `src/client/App.tsx`, `src/client/Scene.tsx` — R3F scene: 44"×30" plane (1 unit = 1 inch, y-up, centred at origin), dark background, OrbitControls, 6" grid via drei `Grid`, top-left HTML overlay (`data-testid="title"`, text "Mallet 42k — M0") |
| Dirs | `src/client`, `src/ai`, `src/data`, `src/assets`, `tools`, `tests/engine`, `tests/e2e` created (placeholder READMEs where empty) |
| Tests | `tests/engine/smoke.test.ts` (Vitest, trivial pass); `tests/engine/contracts.test.ts` (stubs throw, trigger→hook map complete, schemas carry `$id`); `tests/e2e/home.spec.ts` (Playwright, loads page, asserts title, screenshots to `e2e-out/home.png`); `playwright.config.ts` builds + previews on port 4173 under `/mallet-42k/` |
| Scripts | `dev`, `build`, `preview`, `typecheck`, `test`, `test:watch`, `e2e`, `sim` (`tools/sim.mjs`, placeholder prints `sim: not implemented`) |
| CI/CD | `.github/workflows/deploy.yml` — on push to `main`: install, typecheck, test, build, deploy `dist/` to GitHub Pages |
| Docs | `README.md`, `PLAN.md` (source of truth for scope/decisions), this file |
| Engine contracts (M0, stubs only) | `src/engine/types.ts` (GameState, Player, Unit, Model, Runtime{Datasheet,Weapon,Ability}, Board/TerrainPiece/Objective, Phase/PhaseStep, DiceRoll, PendingDecision union of 16 kinds incl. `declareMove`, Rejection codes, EngineInvariantError, GameSetup (`PlayerSetup.enhancementChoice`), PlayerView, SaveFile, ActionLogEntry); `actions.ts` (Action union: 13 decision answers + pass/useStratagem/commandReroll/resign; `WeaponTarget.attacks` for melee splits); `events.ts` (GameEvent union, ~62 types incl. `RoundEnded`, `MoveDeclared`; `ModelDestroyed`/`UnitDestroyed` carry `byModelId`); `hooks.ts` (HookName ×28, typed contexts/results, `CodeHook`/`HookRegistry`, `TRIGGER_HOOK`, `EFFECT_HOOKS`, `hooksForDescriptor` stub); `rng.ts` (`Rng`, `SeededRng`, `ScriptedRng` (serialisable), `createRng`/`restoreRng` — all throw `not implemented`); `decider.ts` (`Decider`, `Deciders`); `index.ts` (`ENGINE_VERSION`, `StepResult`, stubs `createGame/step(state, action, rng?)/legalActions/validate/replay/view`) |
| Data types | `src/data/types.ts` — TS mirror of every JSON schema (`*Data` interfaces, `AbilityDescriptor`, `Condition`, `Effect`, `DataBundle`); engine imports only this from outside itself |
| Data schemas | `src/data/schema/*.schema.json` (copied from `docs/spec/schemas`, JSON Schema 2020-12, `$id` `https://mallet42k.dev/schemas/<name>.schema.json`) + `index.ts` exporting `schemas` map for ajv. TimingWindowId has 28 windows (`round.start/end`, `battle.end`, `movement.moveStarted`, `charge.moveStarted`, `fight.targetsDeclared` added); `Scope.who: bearer`; `Duration: untilEndOfRound`; `Effect.when`, `Effect.move.kind: surge`; `ScoringRule.who: first/second` + rule values `holdEnemyHome/holdNamed/razedThisTurn/claimedSite/claimedSiteConsecutive`; `mission.rules[]` (`MissionRule`); `enhancement.choice` |
| Spec status | Adversarial review round 1 applied (all critical/major + minor items): windows outside turns, kill attribution by model, checklist prefixes unified (`CORE`…`SIM`, `alias` rows, `CORE-001..017` + coverage-gap IDs added), Fire Overwatch retimed to `moveStarted` windows with `declareMove` split, `fight.targetsDeclared`, re-roll offer policy R-6.24, reaction-window policy R-11.5, mission data mapping 11 §2.6, RNG authority (`state.rng`), `Objective.controllerAtTurnStart`/`lootedBy`, `GameResult.reason: tabled` defined |

## Data (M2 — verified, commit 9b278e8)

| Area | State |
|---|---|
| Files | `src/data/{core,missions,terrain,factions/space-marines,factions/orks}` — 28 JSON files; loader `src/data/index.ts` (`loadBundle()`); `tests/data` green; `npm run validate:data` clean (ajv, `tools/validate-data.ts`) |
| Verification | W2-finish 2026-09-13: Orks ok, Space Marines ok, core/missions/terrain ok after fixes. Values checked against `docs/spec/11-combat-patrol.md` and Wahapedia. Terrain `cp-01.json` recomputed so every piece is >1" from every objective in all six missions (min clearance 1.414"). |
| Schema additions | `TargetSpec.state: notYetFought`; `TargetSpec.filter.within.of: controlledObjective` (in `src/data/schema/common.schema.json`, `docs/spec/schemas/common.schema.json`, `src/data/types.ts`, `20-data-schema.md` §6) |
| Code hooks the engine must implement (`src/engine/code-hooks.ts`, grep `"code"` in `src/data`) | `breakTheirSpirit claimSites counterOffensive deadArdFeelNoPain dutyAndHonour epicChallenge fireOverwatch getStuckInDistance grantBenefitOfCover grenadeMortalWounds heroicInterventionCharge irradiatedPowerCells oathOfMomentPick pistonDrivenBrutality properLootin rapidIngressArrival razeAndRuin retrieveIntelligence sabotageComms shockTactics stompEmPick stompEmScore supplyLines sweepingRaidEndgameBonus tankShockMortalWounds tellyportaGrant veteranInstincts waaaghCall wrathOfTheEmperor` (29) |
| Hook-enforcement notes (schema cannot express) | `epicChallenge`: enemy unit (`targets[0]`) must have a leader attached. `tankShockMortalWounds`: VEHICLE model (`targets[2]`) must belong to the charging unit (`targets[0]`). `counterOffensive`: also require Engagement Range (target only encodes `notYetFought`). `veteranInstincts`: re-rolls apply to wound rolls only (1s; any vs MONSTER/VEHICLE). `dutyAndHonour`: objective must be controlled by the active player. |
| Known doc drift | `docs/spec/11-combat-patrol.md` §3 terrain table shows old footprints (data is authoritative); §2.6 names `stompEmPick` as a rule id but data encodes it as `custom` + `code`. |

## Engine (M1 foundations — W1-B terrain/LoS + W1-F hook/effect/stratagem services, commit "M1: foundations")

| Area | State |
|---|---|
| Terrain (`src/engine/terrain.ts`) | `TerrainService` real: `heightAt`, `canEndAt` (barricades never a standing surface at any height; solid crate/HILL footprints and ruin wall panels block ending a move even for models whose keywords let them cross the wall; upper floors need INFANTRY/BEAST/FLY + wholly-within), `crossesImpassable`, `piecesWhollyWithin` (R-3.6 area terrain). Shared geometry (`segmentIntersection2D`, `wallCrosses3D`, `ruinFootprintBlocks` §3.7 both-ends-outside gate, `crateBlocks`, `acrossBarricade` R-3.9) used by `los.ts` too |
| LoS (`src/engine/los.ts`) | `LosService` real: `visible`/`fullyVisible` (model blockers are true elliptical cylinders — the full segment/ellipse intersection interval is checked against the blocker's height range, not just the 2D-closest point, so a ray can graze past an oval base's narrow side or clip a tall blocker's upper cylinder correctly), `unitVisible`/`unitFullyVisible` (attached-leader halves per LEAD-004), `benefitOfCover` (R-3.11–3.14, per-piece attribution via `pieceObstructs` so cover is never granted by unrelated terrain elsewhere), `plungingFire` (R-3.8) |
| Hooks (`src/engine/hooks-impl.ts`, `src/engine/code-hooks.ts`) | `HookService.collect/run/apply` dispatch every active datasheet/leader/enhancement ability + `ActiveEffect` at a hook point, scope-aware (bearer/self/attacker/target/friendly-within/enemy-within); `code-hooks.ts` implements all 29 named code hooks from data (ability, stratagem legality/effect, mission placeholders) plus the reaction-request queue (`pushReaction`/`pendingReactions`/`consumeReaction`) phase modules will read for Fire Overwatch etc. |
| Effects (`src/engine/effects.ts`) | `EffectService.grant/expire/activeFor` — duration-scoped grants (`untilEndOf*`, `nextOwnTurn`) expired at phase/turn/round boundaries and turn start |
| Stratagems (`src/engine/stratagems.ts`) | `stratagemWindow`/`reactionWindow`/`commandReroll` decisions fully handled: affordability + legality (targets, once-per-phase/turn/battle limits, R-1.6 no re-roll of an already-rerolled die), CP spend, effect application via hooks/effects, `StratagemUsed`/`StratagemWindowClosed` events; window offer order (reaction before stratagem, repeat while something is usable) |
| Leaders (`src/engine/leaders.ts`) | Attach/detach bookkeeping, `combinedModels`/`halves` for coherency, targeting and LoS/cover treating an attached pair as one unit |
| Enhancements (`src/engine/enhancements.ts`) | Bearer-scoped ability lookup for the warlord's enhancement, including Tellyporta-style `choice` pairs |
| Tests | `tests/engine/{terrain?,los,los.verify,hooks,hooks.strat,hooks.verify}.test.ts` — 282 tests total, all green (`npm test`); `npm run typecheck` clean |
| Fixed during verification | `los.ts` model-blocker test replaced (2D-closest-point + bounding-circle radius → real ellipse/segment interval vs. height-range overlap: LOS-004-slope, LOS-004-oval); `terrain.ts` `canEndAt` barricade/solid-overlap checks replaced (`pointInPolygon` on the base centre only → `partlyWithinPolygon` so an overhanging base is caught; barricade check no longer skipped at ground level; new wall-panel occupancy check so a model can't end a move inside a ruin wall it's otherwise allowed to cross: LOS-030-end, LOS-022-inside-overlap, LOS-010-wall-end). `pointToSegment` exported from `geometry.ts` for the new wall check |

## Engine (M1 systems — W1-C command/movement/transports + W1-D attack/weapons + W1-F setup deployment/objectives/missions, commit "M1: systems")

| Area | State |
|---|---|
| Command (`src/engine/phases/command.ts`) | Real: CP gain, Battle-shock tests/recovery, `command.start`/`command.battleShock`/`command.end` windows |
| Movement (`src/engine/phases/movement.ts`) | Real: `chooseUnitToActivate` → `declareMove` → `moveUnit` per unit; Normal/Advance/Fall Back/Stationary, Desperate Escape (R-4.6b), coherency cull inputs, Reserves arrivals (Deep Strike/Rapid Ingress) via `deployUnit` at `movement.reinforcements`, end-of-round-3 stranded-Reserves destruction (R-5.14) that now also destroys embarked passengers rather than skipping them, `movement.moveStarted`/`unitMoved` windows (Fire Overwatch), Desperate-Escape surge move helper (`resolveSurgeMove`, exported for the Shooting phase's `shockTactics`/`pistonDrivenBrutality` hooks) with a path-crosses-enemy-model check matching normal moves |
| Transports (`src/engine/transports.ts`) | Real: `embark`/`disembark`/`canEmbark` (capacity, 3" proximity, friendly-only, not this-phase-disembarked, not this-phase-arrived-from-Reserves, not Remained Stationary), `canDisembarkAt`, destroyed-transport passenger handling |
| Attack sequence (`src/engine/attack.ts`) | Real shared sequence used by (future) shooting/charge/fight: hit→wound→allocate→save→damage per model/weapon/target group, Sustained/Lethal/Devastating Wounds (deferred criticals resolve once every group *against that target* is done, before any attack on a different target — R-6.6), Anti-X, re-roll offers (R-6.24), Feel No Pain, Deadly Demise (R-10.4, includes the exploding model's own unit), Hazardous, mortal-wound queue (`queueMortalWounds`) shared with Tank Shock/Grenade/Deadly Demise |
| Weapons (`src/engine/weapons.ts`) | `effectiveWeapon`/ability-value helpers the attack sequence and movement (Heavy stationary bonus) read |
| Setup/deployment (`src/engine/setup.ts`) | Real: P6 alternating `deployUnit` (Defender first, CP-1.10), Reserves/Deep Strike opt-out, attached Leader+Bodyguard as one drop, P7 first-turn (fixed/roll-off/attacker's `chooseOption`), P8 Scouts moves alternating one unit at a time from the first-turn player |
| Objectives/missions (`src/engine/objectives.ts`, `src/engine/missions.ts`) | Real: `evaluateControl` (secure/break CP-2.4/2.5, sticky CP-2.6, claims), scoring-moment live control (`liveController`/`holds`/`controls` all agree: secured-but-just-out-LoC'd breaks at a Command-phase-end evaluation same as `evaluateControl`; 0-vs-0 with nobody ever having been near the marker still trusts the stored controller for isolated scoring-math setups, but not once anything in the battle has actually been destroyed — R-12.3), all 29 named mission code hooks, CP economy, game end/`finalResult` |
| Tests | `tests/engine/{command,movement,movement.verify,attack,attack.verify,missions,missions.verify}.test.ts` — 558 tests total, all green (`npm test`); `npm run typecheck` clean |
| Fixed during verification | `attack.ts`: Deadly Demise wrongly excluded the exploding model's own unit from "every unit within 6\""; Devastating Wounds criticals were deferred past *every* group in the sequence (any target) instead of just the rest of their own target's groups, so a normal attack on target 2 could resolve before target 1's mortal wounds. `missions.ts`: `liveController` trusted a stale `securedBy`/`controller` even when it had just been broken this same evaluation, and non-holdObjectives rules (`properLootin` etc.) read the raw stale `controller` field instead of the live view. `setup.ts`: Scouts alternation ran all of one player's units before the other's instead of alternating one at a time. `transports.ts`: `canEmbark` didn't check "arrived from Reserves this phase" or "Remained Stationary". `movement.ts`: stranded-Reserves culling skipped embarked passengers entirely (leaving them stuck in Reserves forever, or orphaned if the transport died some other way); the `movement.unitMoved` window's re-entry threw `EngineInvariantError` because the per-unit-move marks it needs were already cleared before the window opened; `resolveSurgeMove` didn't check for an intervening enemy model blocking the path. |

## Engine (M1 phases — W1-D/E shooting/charge/fight, commit "M1: phases")

| Area | State |
|---|---|
| Shooting (`src/engine/phases/shooting.ts`) | Real: `selectUnit` → `declareTargets` → `resolve` per unit. Eligibility/legality above the shared attack sequence — Engagement Range/Pistol/Big Guns Never Tire exceptions (R-6.2/6.3/6.7), range+LoS with Lone Operative's 12" cap and Indirect Fire's visibility waiver (never for Torrent), Blast's "not within ER of any unit in the firing player's army", one profile per profileGroup per activation, Pistol-xor-other-weapons per model. Big Guns Never Tire's -1 and Indirect Fire's -1/no-visible-model cover are snapshotted at `attack.begin` (target declaration), not re-evaluated live. Drives `services.attack` through `shooting.targetsDeclared` (defensive-first)/`shooting.attacksResolved`. Fire Overwatch itself is resolved by movement.ts/charge.ts calling `services.attack` directly with `overwatch: true` |
| Charge (`src/engine/phases/charge.ts`) | Real: `declareCharge` → 2D6 roll (`ctx.rollOnce`, Command Re-roll via `any.rollMade`) → `charge.moveStarted` (Fire Overwatch, then R-8.4 feasibility re-check) → `chargeMove` → `charge.moveEnded` (Heroic Intervention reaction, then Tank Shock stratagem). `planChargeMove` heuristic: direct approach to nearest declared target with detour-around-non-target-ER, then angular sweep around a target's ER ring to avoid self-overlap, then walk-to-nearest-friend fallback for coherency — capped by roll/terrain/board/overlap legality, not an exhaustive combinatorial search. A LIFO stack (`ch:meStack`) of units awaiting `moveEnded` draining lets a Heroic Intervention's own landed move re-enter the same machinery (and itself trigger Tank Shock) across `advance()` re-entries |
| Fight (`src/engine/phases/fight.ts`) | Real: Fights First then remaining units (R-9.1/9.3), alternating `chooseFightUnit` via `nextToSelect`, per-unit `pileIn` → `declareTargets` (`fight.targetsDeclared`, defensive-first) → attacks (shared `attack.ts`) → `consolidate`. Counter-offensive reaction inserts a named unit ahead of ordinary alternation and restores the pre-reaction `nextToSelect` afterward (pure insertion; R-9.12 is silent on this — documented interpretation). Pile-in/consolidate share one heuristic approach-nearest-enemy-point search with charge's, same non-exhaustive caveat |
| Fixed landing this stage | `attack.ts`'s Indirect Fire cover grant (`finalizeAllocation`) was recomputing target visibility live at allocation instead of reading the `indirectNoLos` mark frozen at target declaration — a target that moved into view between declare and allocate wrongly lost the cover benefit it's entitled to for having been unseen when targeted (SHOOT-041-cover-snapshot). Now reads the same snapshot mark the -1-to-hit modifier already used |
| Tests | `tests/engine/{shooting,shooting.verify,charge,fight,fight.verify}.test.ts` — 665 tests total, all green (`npm test`); `npm run typecheck` clean |

## Engine (M1 integrated — W1-G headless sim, commit "M1: rules engine integrated, headless sim")

| Area | State |
|---|---|
| Simulator | `tools/sim-core.ts` (`runGame`, `makeSimSetup`) + CLI `tools/sim.ts` (`npm run sim -- --games N --seed S [--verbose]`, tsx). Two random-legal-move bots over `createGame`/`legalActions`/`step` with the real bundle: both Combat Patrol rosters (sides alternate per game), missions cp-01..06 cycled, terrain cp-01, roll-off sides/first turn. Invariants after every step: no NaN positions/wounds/CP/VP, wounds in (0, W] for live models, destroyed units have no models, CP >= 0, round <= 5, phase/turn order never goes backwards (between-turn `round.*`/`turn.*`/`battle.*` windows excluded), every pending decision has >= 1 legal action and it is accepted, game ends with a `GameResult`. Violations print seed + action index; exit code 1 |
| Result | `npm run sim -- --games 100 --seed 1`: **0 violations**; wins ork 71 / sm 18, draws 11; mean rounds 5.00; mean actions/game 281.2; 789 s (~7.9 s/game) |
| legalActions for continuous decisions | `src/engine/phases/legal.ts` shared helpers (`optionActions`, `filterValid`, `translatePlacements`, `formationPlacements`, `repairCoherency`). Movement: `moveUnit` (rigid translations toward objective / toward / away from nearest enemy / 16 compass, formation and coherency-repair regroups, stay) and Reserves `deployUnit` (grid scan >9" from enemies); Charge: `declareCharge` (one per candidate target), `chargeMove`; Fight: `pileIn`/`consolidate`/melee `declareTargets`; Shooting: ranged `declareTargets` (one declaration per target unit + pass); Setup `deployUnit` now validated with a compact-block fallback. All candidates are filtered through the module's own validation |
| Fixed this stage | Charge feasibility (`chargeFeasible`) and Fight pile-in/consolidate feasibility used only the planner's arrangement, which could pass placement checks yet fail R-8.5/R-9.5 "must end in base contact if possible" → engine raised a `chargeMove`/`pileIn` with no legal answer. Feasibility now = "some candidate passes full validation" (`legalChargeMoves`, `legalApproachMoves`), so an impossible charge fails at the roll. `planChargeMove`'s `tryChargePlacement` never checked end-position overlap with enemy models. Charge re-roll offer/auto re-roll could re-roll a die already Command Re-rolled (R-1.6, threw `applyReroll`). Setup auto-deploy raster could wrap a unit across a row gap (out of coherency). `geometry.baseEdgePoints` memoized (oval gap tests were ~85% of sim time). [interp R-2.6] `movement.ts` `moveRejection`: a unit already out of coherency (mid-turn casualties, e.g. Fire Overwatch at `movement.moveStarted`) that moves no model may end its declared move as it stands — otherwise a declared Normal/Advance move could have no legal answer; the end-of-turn cull resolves it. Coherency-repair candidates check terrain (`canEndAt`/`crossesImpassable`) |
| Tests | `tests/engine/fullgame.test.ts` — 3 seeded full games (SIM-001/002/003/007/009/015): no violations, `GameResult`, `replay(log)` reproduces the final hash. 669 tests green; `npm run typecheck` clean |

## Client (M3 — first playable client (v0), + playtest fixes)

| Area | State |
|---|---|
| Screens (`src/client/App.tsx`) | Start screen (faction/mission/opponent/seed) → 3D game screen (canvas + HUD/prompt overlays) → end screen layered over the still-rendered board. `StartScreen` builds a real `GameSetup` from `loadBundle()` and calls `useGameStore.newGame()`. Dev/QA-only `?gallery` query flag (optional `&pose=`/`&faction=`) renders `FigureGalleryStage` instead of the game, for figure-kit screenshots |
| Store (`src/client/store/game.ts`) | Zustand store: owns the one `GameState`, drives it only through `engine.step()` (never mutates state directly), keeps `pending`/`legal`/event/dice/action logs, schedules the bot seat (`src/ai/random.ts`'s `RandomDecider`) on a short delay, save/load via `localStorage` (`engine.save`/`load`) |
| AI (`src/ai/random.ts`) | `RandomDecider` implements the engine's `Decider` interface — uniform pick over `legalActions()` with a bias against `pass` when another action is legal; used for the bot seat and in `tests/client/store.test.ts` |
| Scene (`src/client/Scene.tsx`, `src/client/board/*`) | R3F canvas: `Board` (44"x30" mat, grid, deployment-zone tint), `Terrain` (block/wall pieces height-capped at 3.2 units and rendered semi-transparent so they never fully hide units behind them — cover/LoS still use the real uncapped height via the engine's terrain service), `Objectives` (control-coloured rings), `Lighting`, `CameraRig` (orbiting camera, top-down toggle, clamped above the mat, ~30% closer default start distance so figures read at a legible size) |
| Figures (`src/client/figures/*`) | Procedural chibi/SD figures built from datasheet base size + a small per-archetype kit config (`BipedBody`/`VehicleBody`/`BaseDisc`, `kitConfigs.ts`) — original designs, not GW sculpts; vehicle default height bumped (0.95→1.8) so chassis/rotor fill their base instead of reading as a bare disc; `FigureGallery`/`FigureGalleryStage` wired behind the `?gallery` flag above, row layout now spaces entries by each one's own base radius instead of a fixed pitch, so a Dread/Warboss/Deffkopta no longer overlaps its neighbour |
| Interaction (`src/client/interaction/*`) | `UnitsLayer` (one `<Figure>` per live model, click-to-activate/target via `decisions.ts`, models ease toward their engine position each frame — `EasedGroup` — instead of snapping, and hold a brief shoot/melee pose read off `AttackSequenceStarted` events since a whole attack resolves inside one `step()`), `UnitLabels` (floating name + total-wounds tag over every on-board unit), `boardClick.ts`/`geometry.ts`'s `combinedUnitModels` (board click → placement draft for deployment and the four move-family decisions, now including an attached leader/bodyguard's models so a click-driven move can't strand it out of coherency), `PlacementOverlay` (ghost preview + range ring; a draft or hovered suggested-placement preview renders red when `placementsOverlapExisting` says it would overlap a model already on the board, gold/blue otherwise), `DeathGhosts` (fade-out on model/unit destruction) |
| UI (`src/client/ui/*`) | `Hud` (round/phase tracker, CP/VP, end-phase/pass), `DecisionPrompt` (bespoke deploy/move controls plus a generic clickable-option list so no decision kind can get stuck; move/charge/pile-in/consolidate options now show a plain-language summary — `"3.4\" toward Boyz"` — and hovering one previews its placement as a ghost via `previewDraft`; charge options show the inches still needed), `UnitCard` (now has a close button and auto-clears when the phase advances, so it can't get stuck open), `DiceLog` (collapsed by default, capped/scrollable, each roll described as an attributed sentence — `"Boyz vs Intercessors — To hit"` — instead of a bare purpose/number pair), `EventFeed` (filtered to events a player actually cares about — internal bookkeeping like `HitRolled`/`DecisionRequested` no longer prints as a raw type name — and describes more of them: reinforcements arriving, damage with attacker attribution, battle-shock recovery, charge declarations/rolls, objectives secured, VP scored), `Toast` (rejection codes), `EndScreen` |
| How to play | `npm run dev`, pick a faction/mission/opponent (Bot or Hotseat) and Start — alternate `deployUnit` picks (click a unit chip, then click inside your deployment zone), then play phase-by-phase; the HUD's "End phase / Pass" button also serves as the `pass` action; a bot seat plays itself on a short delay |
| Tests | `tests/client/store.test.ts` — `newGame()` + `RandomDecider` on both seats drives 300 steps through the real bundle without throwing; `tests/e2e/smoke.spec.ts` — Playwright screenshots of the start screen, an in-progress deployment, and the figure gallery (`e2e-out/*.png`, not committed); `tests/e2e/play.spec.ts` — a human-like full playtest through the real UI (real clicks on unit chips/board/figures/prompt buttons, screen-space projection matching `CameraRig`'s orbit math, reads `window.__mallet` only to locate things and detect a stuck decision, never dispatches directly), screenshots + `e2e-out/play-log.json` |
| Known gaps | Large single JS chunk (1.5 MB, no code-splitting) — fine for now per owner priority ("something playable fast"); no camera-relative screen-space UI for out-of-view units; AI is uniform-random, not remotely strategic; `src/assets` still unused (figures are procedural, not asset-based); pile-in/consolidate/charge-move placement heuristics are non-exhaustive (see engine notes) so the client's suggested-placement list occasionally has no candidate for an unusual geometry, in which case only the fallback list/manual board-click remains |

## Client (M6 — missions, scoring, CP and stratagems visible/playable, commit "M6: missions, scoring, CP and stratagems in the client")

| Area | State |
|---|---|
| Setup (`src/client/ui/StartScreen.tsx`) | Loads the same `DataBundle` the store will use so the mission dropdown shows real names + primary-scoring bullets (`labels.ts` `primaryScoringSummary`) and text before a game starts; a "Your Secondary" picker lets the player choose from their patrol's secondaries (falls back to the patrol default), wired through `NewGameOptions.secondaryId` → `store/game.ts` `buildPlayerSetup` |
| In-game mission briefing (`src/client/ui/MissionPanel.tsx`) | "Mission" button in the HUD toggles a panel with the mission's blurb, primary scoring bullets, any mission special rules (`missionRuleLabel`), and both players' chosen secondary name + text — the M6 gap where the mission dropdown showed a bare id and neither secondary was ever explained in-game |
| Stratagems (`src/client/ui/StratagemPanel.tsx`, `stratagemInfo.ts`) | Bottom-left "Stratagems (N CP)" tab lists every stratagem the active/pending player owns (core + faction), each with cost, a plain-language timing line, its effect text, and — via a client-side approximation of the engine's eligibility (CP, active/reactive, phase, once-per-battle/round/turn/phase limits, Sabotage Comms lock) — greyed out with a reason when not usable right now. `DecisionPrompt.tsx`'s `stratagemWindow`/`reactionWindow` prompts now show the same name/cost/effect list plus a trigger line ("Triggered by …") and the player's current CP, instead of bare stratagem ids |
| Scoring visibility (`src/client/store/game.ts` `vpToastFrom`, `src/client/ui/VpToast.tsx`, `Hud.tsx`) | A "+N VP — Reason" pop-up fires on every `VpScored` event (auto-dismiss 3s); each player's HUD VP number is a toggle exposing a per-round scoring breakdown (`hud-vp-breakdown-{A,B}`); a small scoring-window hint ("Scoring: end of your Command phase (rounds 2-4)") sits under the phase tracker; `EventFeed.tsx` now has a separate pinned "Scoring" list (CP/VP lines) above the general "Events" list so they don't scroll out of view between combat lines |
| Objective control (`src/client/board/Objectives.tsx`, `controlLevels.ts`) | Each marker shows a floating label (id, a lock icon once `securedBy` is set, "sticky" tag) plus live OC for both sides (`liveControlLevels` — a client-side mirror of the engine's `levelOfControl`; known gap: doesn't apply ability-based OC modifiers since the engine's real instance isn't exported as a value); a short fading ghost ring marks a just-razed/removed marker; a hovered decision option (raze/recover/stomp/bag target) highlights its objective on the board via `useUiStore.hoverObjective` |
| End screen (`src/client/ui/EndScreen.tsx`) | Adds a round-by-round VP table split into primary vs. secondary per player (`secondaryScoringIds` distinguishes a player's own secondary's rule ids from the mission's shared primary/special-rule scoring) and a "Tabled in round N" / "After N rounds" line next to the win-reason text |
| Battle-shock visibility | `UnitLabels.tsx` tints a battle-shocked unit's label orange and appends "· Shocked"; `UnitCard.tsx` now explains the consequence ("its objective control counts as 0") instead of just naming the state |
| Engine fixes found while wiring this up | `missions.ts` `whoApplies`: `who: 'first'/'second'` scoring rules were double-awarding on round 5 (both `command.end`/`turn.end` fire once per player per round; the rule matched on seat alone, so it also fired on the *other* player's own end-of-phase window) — now also gates on `s.activePlayer` matching. `attack.ts`: model-attributed kills are now tallied per attacking model (`secondaryState.killsThisPhase`) so kill-count secondaries scoped to a specific model (Wrath of the Emperor → the Captain) work off `by.modelId` rather than a unit's live `models[]`, which goes empty once that unit is destroyed |
| Tests | `tests/e2e/m6.spec.ts` — two real-UI Playwright games vs the bot (Scorched Earth cp-04 to the end screen; Clash of Patrols cp-01 for 2+ rounds), asserting no stuck decisions/rejections/console errors and screenshotting `e2e-out/m6-{01-setup,02-objectives,03-scoring,04-stratagem,05-end}.png` (not committed). Last run (2026-09-14): both pass (2.0m / 2.9m); cp-04 played a full 5-round game to a real `GameResult` (Orks win 30-12 VP) using Fire Overwatch, Command Re-roll, Duty and Honour and Veteran Instincts along the way, zero rejections/stuck/console errors |
| Known gaps | Stratagem-panel eligibility is an approximation (doesn't check per-stratagem targets/conditions, so a stratagem with no valid target right now can still show as usable) — the real gate is still the engine's own legality check when the player actually tries to use it; live OC display doesn't include ability-based OC modifiers (see Objective control above) |

## AI (M5 — tier-1 utility AI wired into the client, commit "M5: tier-1 utility AI opponent")

| Area | State |
|---|---|
| Decider (`src/ai/utility.ts`) | `UtilityDecider` implements the engine `Decider` interface — scores every legal action for the current decision with cheap heuristics (docs/spec/40-ai.md §4-5, simplified per owner priority: no Monte Carlo, no role planner, no points data — see `src/ai/expected.ts` header for the specific simplifications). Two tiers: `normal` always takes the top-scored action; `easy` picks randomly among the top 3. Covers deployment, movement (stationary/normal/advance/fall back), charge declarations + charge moves, targeting (`declareTargets`/`allocateAttack`), fight-unit selection, stratagem/reaction windows (fire overwatch, heroic intervention, counter-offensive, and the go-to-ground family are modelled by name; everything else defaults to holding CP per the spec's own stated default), command re-rolls, and generic `chooseOption` decisions via their hint payload. Objective value is derived live from the loaded mission's own scoring rules (`objectiveScoreWeight`), so the AI chases markers that actually pay under whichever mission is in play rather than a flat per-marker constant |
| Expected-value helpers (`src/ai/expected.ts`) | Closed-form hit/wound/save/damage math (no dice simulation) feeding the scorers above: `sumExpectedAttacks`, `unitRangedDamage`/`unitMeleeDamage`, `threatAt`, `unitValue` |
| Client wiring (`src/client/store/game.ts`) | Opponent `'bot'` now drives `UtilityDecider` at the difficulty chosen on the start screen. New `AiDifficulty` type: `'random'` keeps the pre-M5 uniform-random `RandomDecider` as an explicit easy option, `'easy'`/`'normal'` select `UtilityDecider`'s own tiers; default `'normal'` (`DEFAULT_AI_DIFFICULTY`). `botDecider` is now typed as the generic engine `Decider` rather than `RandomDecider`. Bot scheduling is unchanged — a `setTimeout` yield before each bot decision — no Worker was needed; see bench numbers below |
| Start screen (`src/client/ui/StartScreen.tsx`) | New "Bot Strength" picker (`AI_DIFFICULTY_OPTIONS`, own-words labels: "Random (easy)" / "Casual" / "Standard", each with a plain-language blurb), shown only when Opponent = Bot |
| Bench (`tools/ai-bench.ts`, `npm run bench:ai -- --games N --seed S [--difficulty easy\|normal]`) | UtilityDecider vs RandomDecider on the real Combat Patrol rosters, alternating faction/seat, missions cp-01..06 cycled. Last run (`--games 12 --seed 1 --difficulty normal`): `{"games":12,"aiWins":7,"randomWins":5,"draws":0,"meanMs":1.1,"p95Ms":6.8,"rejections":0}` — AI decision time is far below any stall threshold, confirming the plain `setTimeout` yield is enough (no Worker) |
| Tests | `tests/ai/utility.test.ts` — UtilityDecider vs RandomDecider plays a full game through the public engine API without a rejected action; `tests/e2e/play.spec.ts` now also screenshots `e2e-out/ai-01-bot-turn.png` the first time the bot seat has a pending decision during movement or shooting (a 5x `setTimeout` pacing stretch on that one page — store.ts's real `BOT_DELAY_MS` is untouched — widens the window past the bot's own ~400ms decision timer so the shot reliably lands on "Opponent is thinking…" instead of whatever the bot's move triggers next, e.g. a Fire Overwatch reaction offered to the human). Last run: `npm run e2e -- play.spec.ts` passed (10.1m; the pacing stretch adds real wall-clock time to this one spec — `npm test`'s own AI test is unaffected). `npm run typecheck` / `npm test` (670 tests) / `npm run build` all green |
| Known gaps | Only `'easy'`/`'normal'` UtilityDecider tiers exist — the spec's `hard`/Monte Carlo tier (§6-7) isn't built, per owner priority "simple and strong beats complete"; stratagem/reaction scoring only models a handful of named stratagems by id match; no multi-turn lookahead — every decision is scored greedily on its own |

## Client (M8 — dice animation, combat VFX, sound/narration, figure animation, commit "M8: dice animation, combat VFX, sound and narration, figure animation")

| Area | State |
|---|---|
| Dice (`src/client/dice/**`) | `<DiceTray/>` (bottom-centre HTML overlay, `data-testid="dice-tray"`) + `playRoll(RollRequest)` queue (`playRoll.ts`/`diceStore.ts`): pipped d6s tumble and settle, glow on success/dim on fail/pop on a natural 6, batches over 8 dice (e.g. a 20-shot Ork volley) collapse to a compact "N dice: M hits" row, re-rolled dice flip visibly. Shared `DiceSpeed` ('normal'/'fast'/'instant', `setSpeed`/`getSpeed`) — 'instant' skips the tumble/hold entirely (settles same tick), so it isn't the speed to use if you want to *see* a roll (see Tests below). |
| VFX (`src/client/vfx/**`) | `<VfxLayer/>` — 5 fixed-capacity `InstancedMesh` pools (spark/ring/puff/tracer/cone), no per-effect allocation after warm-up — + imperative `vfx.{shoot,hit,save,melee,death,chargeDust,objectivePulse,mortal}`. Tracers/muzzle flash coloured per weapon kind (bolter/shoota/heavy/flame/psychic, `palette.ts`), impact sparks+flash ring, shield-flicker saves, melee sparks+chop-tracers in the attacker's faction colour, death dust+debris, charge-dust puffs along the move path, objective-capture pulse rings, purple mortal-wound burst. Every effect ≤0.8s. |
| Audio (`tools/gen-audio.ts`, `tools/audio-manifest.json`, `public/audio/**` (~880KB), `src/client/audio/**`) | 40 ElevenLabs-generated assets (28 SFX + 12 narrator lines + 1 ambient loop, catalogued in `manifest.ts`) via an idempotent generator (skips files already on disk; key read from `ELEVENLABS_API_KEY` env only — never written, logged, or committed; verified clean, see Verification below). `AudioManager` (`manager.ts`): lazily-unlocked Web Audio context, master/sfx/voice/music gain nodes with volume+mute persisted to `localStorage` (`settings.ts`), per-id throttling (max 4 concurrent instances, min retrigger gap per channel) so a 20-shot volley layers instead of clipping, ambient loop streamed via `<audio>` rather than decoded. `eventSounds.ts` maps engine `GameEvent`s to sound ids (pure, no side effects); `useAudioSettings.ts` is the settings-panel React binding. |
| Figure animation (`src/client/figures/**`) | `<Figure/>` now eases its own `position`/`rotationY` every frame (snapping only on first mount) and infers the walk cycle from that motion, so callers just set target state — no wrapper needed (`interaction/UnitsLayer.tsx`'s old `EasedGroup` wrapper is gone, folded into `Figure` itself). New `action` prop (`FigureAction: {kind, t0}`, `t0` compared only by identity so a timestamp/seq/counter all work) layers a one-shot `'shoot'`/`'melee'`/`'hit'`/`'death'` cue over idle/walk and times its own reversion (shoot/melee ~700ms, hit ~300ms; death is terminal, never reverts). Real per-pose motion: idle sway; walk (leg/arm swing, torso counter-twist, footfall bounce — heavier `bulk` kits like Terminators/the Dread get a slower, harder-stomping stride, no per-kit special-casing); shoot recoil; melee lunge-and-swing (one lunge-and-recover cycle per swing); hit flinch (decaying shake+knockback); death topple + opacity fade + sink (`useMaterialFader`, shared by biped and vehicle bodies). Deffkopta (vehicle) additionally gets rotor spin/hover bob and a ramming lunge for melee. Full per-model API documented at the top of `src/client/figures/index.ts`. |
| Presentation director (`src/client/presentation/**`, new) | `startDirector()` subscribes to the game store's event log and plays each fresh batch of engine events in order, mounted once via `<PresentationDirector/>` in `App.tsx`: dice rolls through `playRoll`, shots/hits/saves/melee/death/objective/mortal through `vfx.*`, matching sounds via `eventSounds.ts`, figure shoot/melee/hit action cues + a brief "turn to face the target" facing override via a small `cueStore` that `UnitsLayer.tsx` reads directly (replacing the old poll-the-event-log pose hack), narrator phase lines capped at once per phase per round. Paced by `usePresentationSettings` (`animSpeed`, same 'normal'/'fast'/'instant' vocabulary as the dice speed — fixed inter-event gaps in `EVENT_GAP_MS`, skipped at instant/scaled ×0.35 at fast); one event's presentation failure is caught and logged, never breaks the rest of the queue. |
| Settings popover (`src/client/ui/SettingsPanel.tsx`, `ui/Hud.tsx` "Settings" button, `ui/uiStore.ts`) | Audio mix (master/sfx/voice/music sliders + mute) and Presentation (animation-speed 3-way, dice-animation on/off, ambient-sound on/off) — both halves persisted independently (`audio/settings.ts`, `presentation/settings.ts`) and applied on load without needing the panel opened. `App.tsx` also now unlocks audio on the player's first click/tap/key anywhere, and mounts `<VfxLayer/>` under the Canvas plus `<DiceTray/>`/`<SettingsPanel/>` in the HTML overlay. |
| Verification | `npm run typecheck` / `npm test` (671 tests, unchanged — no new unit tests for this presentation-only milestone) both clean. `npm run build`: single JS chunk grew **1.5MB → 1.66MB** (dice/vfx/audio/presentation/animation code; audio itself is static assets under `public/audio`, not bundled into the chunk) — still one chunk, no code-splitting, per owner priority ("visible, fun polish fast" over bundle hygiene). `grep -rE 'sk_[A-Za-z0-9_-]{20,}' src tools public tests STATUS.md` — no match; the ElevenLabs key is read from `ELEVENLABS_API_KEY` at generation time only (`tools/gen-audio.ts`), never touches the repo. |
| e2e (`tests/e2e/play.spec.ts`) | Extended, selectors/timing only: right after game start it opens Settings, screenshots the popover (`m8-04-settings.png`), and switches animation speed to **`fast`** (not `instant` — instant settles a roll in the same tick with 0ms tumble/hold, which would make "dice tray mid-roll" and "tracers/impacts mid-shooting" screenshots structurally impossible to land; `fast` still keeps the whole run inside its existing 10-minute game budget, unchanged from pre-M8). The rest of the playtest loop is untouched; it opportunistically also captures `m8-01-shooting.png` (~180ms after a target is declared, to catch the tracer/impact vfx), `m8-02-dice.png` (polled every loop iteration for `dice-tray` visibility — the director trails a beat behind the engine's `pending` state, so the tray is often still up well after the loop's own state has moved on), and `m8-03-melee.png` (first frame with `phase === 'fight'`, else a fallback frame at the end — this run reached Fight phase in rounds 1-2 via Fire Overwatch charges, so no fallback was needed). Last run (2026-09-14): passed, 10.2m (`elapsedS: 601`, hit the same 10-minute game budget as every prior run at this budget — the human played 3 rounds: deployment, 3 board-click moves, 3 ranged targets declared, 4 charges declared; `stuck: []`, `consoleErrors: []`); all four m8-*.png read back correctly rendered (visible dice tray with a live roll, tracer-lit shooting frame with a Command Re-roll prompt, a Fight-phase frame with resolved melee damage in the event feed, and the Settings popover with every control legible) — no retake needed. |
| Known gaps | Settings speed is a 3-way toggle only, no fine slider; melee/psychic vfx share one shape across kinds (only colour varies); the ambient loop is a single fixed ~20s track with no crossfade; narrator lines are one fixed voice/accent; `public/audio` (~880KB) is now the single biggest asset addition this milestone and is not lazy-loaded (every id is playable from the very first click, decoded eagerly except the streamed ambient loop). |

## Not yet built

- Anything beyond M8 (see Client tables for what M3/M6/M8 cover); AI Monte Carlo/hard tier, generated models, scale-up, multiplayer, Phase B glTF animation/asset pipeline for figures (see PLAN.md, docs/spec/30-figures.md §9)

## Notes for the next agent

- `npm run typecheck` / `npm test` / `npm run build` all pass as of M0 (see verification log at commit time).
- `npm run e2e` requires `npx playwright install chromium` once per machine/CI runner.
- Engine contracts are frozen: change `src/engine/types.ts`/`actions.ts`/`events.ts`/`hooks.ts` only with a matching edit to `docs/spec/00-architecture.md`.
- Movement is two decisions per unit (`declareMove` → `moveUnit`); Fire Overwatch windows are `movement.moveStarted`/`unitMoved`/`reinforcements` and `charge.moveStarted`/`moveEnded` (10-rules R-5.1, R-8.3, R-11.5). Optional re-rolls follow R-6.24 (`rerollOffer`).
- Data shape types live in `src/data/types.ts`; runtime (resolved) shapes in `src/engine/types.ts` (`Runtime*`). Pre-game choices (patrol, enhancement + `enhancementChoice` unit ref, secondary, leader attachments, reserves) are part of `GameSetup`, not decisions; side/first-turn roll-offs surface as `chooseOption` decisions.
- `createGame` now genuinely stops at the first `deployUnit` decision (real deployment, W1-F) — any test/fixture that used to assume a stub deployment completed instantly for free must drive real `deployUnit`/`moveUnit`(Scouts)/`chooseOption` decisions to get past it first; see `tests/fixtures/modules.ts`'s `deployAll` helper (drives every decision whose `window` is `'deployment.unit'` using its own default legal action) and its many call sites in `tests/engine/core*.test.ts`.
- All five phase modules (command/movement/shooting/charge/fight) plus setup/deployment are now real — the engine plays a complete game end-to-end through `step`/`legalActions`. W1-G headless sim is done (see "Engine (M1 integrated)"); `legalActions` now returns >= 1 concrete answer for every decision kind, so AI/autoplay can drive whole games. Sim is slow (~6 s/game, oval-base gap sampling in charge ring search) — worth an analytic ellipse distance later.
- `terrain.canEndAt`, `los.ts`'s model-blocker test, several M1-systems modules, and now `attack.ts`'s Indirect Fire cover snapshot (see "Fixed landing this stage" above) were adversarially wrong on first pass — worth a similar adversarial pass on Shooting/Charge/Fight's own heuristics (`planChargeMove`, `planApproachEnemy`) before trusting them past their own happy-path tests.
