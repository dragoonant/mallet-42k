# Status

Maintained by agents as work lands. Orient from this file instead of exploring the codebase.

## What exists (M0 — project scaffold)

| Area | State |
|---|---|
| Build | Vite + React + TypeScript (strict), package `mallet-42k`, ESM (`"type": "module"`) |
| Deps | three, @react-three/fiber, @react-three/drei, zustand; dev: typescript, vite, @vitejs/plugin-react, vitest, @playwright/test, @types/* |
| Config | `vite.config.ts` (base `/mallet-42k/` in prod, `/` in dev; `@` → `src`; vitest env `node`, `tests/**/*.test.ts`); `tsconfig.json` (strict, includes `src` + `tests`, works with or without `src/engine`) |
| Client | `src/main.tsx`, `src/client/App.tsx`, `src/client/Scene.tsx` — R3F scene: 44"×30" plane (1 unit = 1 inch, y-up, centred at origin), dark background, OrbitControls, 6" grid via drei `Grid`, top-left HTML overlay (`data-testid="title"`, text "Mallet 42k — M0") |
| Dirs | `src/client`, `src/ai`, `src/data`, `src/assets`, `tools`, `tests/engine`, `tests/e2e` created (placeholder READMEs where empty) |
| Tests | `tests/engine/smoke.test.ts` (Vitest, trivial pass); `tests/engine/contracts.test.ts` (stubs throw, trigger→hook map complete, schemas carry `$id`); `tests/e2e/home.spec.ts` (Playwright, loads page, asserts title, screenshots to `e2e-out/home.png`); `playwright.config.ts` builds + previews on port 4173 under `/mallet-42k/` |
| Scripts | `dev`, `build`, `preview`, `typecheck`, `test`, `test:watch`, `e2e`, `sim` (`tools/sim.mjs`, placeholder prints `sim: not implemented`) |
| CI/CD | `.github/workflows/deploy.yml` — on push to `main`: install, typecheck, test, build, deploy `dist/` to GitHub Pages |
| Docs | `README.md`, `PLAN.md` (source of truth for scope/decisions), this file |
| Engine contracts (M0, stubs only) | `src/engine/types.ts` (GameState, Player, Unit, Model, Runtime{Datasheet,Weapon,Ability}, Board/TerrainPiece/Objective, Phase/PhaseStep, DiceRoll, PendingDecision union of 16 kinds incl. `declareMove`, Rejection codes, EngineInvariantError, GameSetup (`PlayerSetup.enhancementChoice`), PlayerView, SaveFile, ActionLogEntry); `actions.ts` (Action union: 13 decision answers + pass/useStratagem/commandReroll/resign; `WeaponTarget.attacks` for melee splits); `events.ts` (GameEvent union, ~62 types incl. `RoundEnded`, `MoveDeclared`; `ModelDestroyed`/`UnitDestroyed` carry `byModelId`); `hooks.ts` (HookName ×28, typed contexts/results, `CodeHook`/`HookRegistry`, `TRIGGER_HOOK`, `EFFECT_HOOKS`, `hooksForDescriptor` stub); `rng.ts` (`Rng`, `SeededRng`, `ScriptedRng` (serialisable), `createRng`/`restoreRng` — all throw `not implemented`); `decider.ts` (`Decider`, `Deciders`); `index.ts` (`ENGINE_VERSION`, `StepResult`, stubs `createGame/step(state, action, rng?)/legalActions/validate/replay/view`) |
| Data types | `src/data/types.ts` — TS mirror of every JSON schema (`*Data` interfaces, `AbilityDescriptor`, `Condition`, `Effect`, `DataBundle`); engine imports only this from outside itself |
| Data schemas | `src/data/schema/*.schema.json` (copied from `docs/spec/schemas`, JSON Schema 2020-12, `$id` `https://mallet42k.dev/schemas/<name>.schema.json`) + `index.ts` exporting `schemas` map for ajv. TimingWindowId has 28 windows (`round.start/end`, `battle.end`, `movement.moveStarted`, `charge.moveStarted`, `fight.targetsDeclared` added); `Scope.who: bearer`; `Duration: untilEndOfRound`; `Effect.when`, `Effect.move.kind: surge`; `ScoringRule.who: first/second` + rule values `holdEnemyHome/holdNamed/razedThisTurn/claimedSite/claimedSiteConsecutive`; `mission.rules[]` (`MissionRule`); `enhancement.choice` |
| Spec status | Adversarial review round 1 applied (all critical/major + minor items): windows outside turns, kill attribution by model, checklist prefixes unified (`CORE`…`SIM`, `alias` rows, `CORE-001..017` + coverage-gap IDs added), Fire Overwatch retimed to `moveStarted` windows with `declareMove` split, `fight.targetsDeclared`, re-roll offer policy R-6.24, reaction-window policy R-11.5, mission data mapping 11 §2.6, RNG authority (`state.rng`), `Objective.controllerAtTurnStart`/`lootedBy`, `GameResult.reason: tabled` defined |

## Data (M2 — verified, commit 9b278e8)

| Area | State |
|---|---|
| Files | `src/data/{core,missions,terrain,factions/space-marines,factions/orks}` — 28 JSON files; loader `src/data/index.ts` (`loadBundle()`); `tests/data` green; `npm run validate:data` clean (ajv, `tools/validate-data.ts`) |
| Verification | W2-finish 2026-09-13: Orks ok, Space Marines ok, core/missions/terrain ok after fixes. Values checked against `docs/spec/11-combat-patrol.md` and Wahapedia. Terrain `cp-01.json` recomputed so every piece is >1" from every objective in all six missions (min clearance 1.414"). |
| Schema additions | `TargetSpec.state: notYetFought`; `TargetSpec.filter.within.of: controlledObjective` (in `src/data/schema/common.schema.json`, `docs/spec/schemas/common.schema.json`, `src/data/types.ts`, `20-data-schema.md` §6) |
| Code hooks the engine must implement (`src/engine/code-hooks.ts`, grep `"code"` in `src/data`) | `breakTheirSpirit claimSites counterOffensive deadArdFeelNoPain dutyAndHonour epicChallenge fireOverwatch getStuckInDistance grantBenefitOfCover grenadeMortalWounds heroicInterventionCharge irradiatedPowerCells oathOfMomentPick pistonDrivenBrutality properLootin rapidIngressArrival razeAndRuin retrieveIntelligence sabotageComms shockTactics stompEmPick stompEmScore supplyLines sweepingRaidEndgameBonus tankShockMortalWounds tellyportaGrant veteranInstincts waaaghCall wrathOfTheEmperor` (29) |
| Hook-enforcement notes (schema cannot express) | `epicChallenge`: enemy unit (`targets[0]`) must have a leader attached. `tankShockMortalWounds`: VEHICLE model (`targets[2]`) must belong to the charging unit (`targets[0]`). `counterOffensive`: also require Engagement Range (target only encodes `notYetFought`). `veteranInstincts`: re-rolls apply to wound rolls only (1s; any vs MONSTER/VEHICLE). `dutyAndHonour`: objective must be controlled by the active player. |
| Known doc drift | `docs/spec/11-combat-patrol.md` §3 terrain table shows old footprints (data is authoritative); §2.6 names `stompEmPick` as a rule id but data encodes it as `custom` + `code`. |

## Engine (M1 foundations — W1-B terrain/LoS + W1-F hook/effect/stratagem services, commit "M1: foundations")

| Area | State |
|---|---|
| Terrain (`src/engine/terrain.ts`) | `TerrainService` real: `heightAt`, `canEndAt` (barricades never a standing surface at any height; solid crate/HILL footprints and ruin wall panels block ending a move even for models whose keywords let them cross the wall; upper floors need INFANTRY/BEAST/FLY + wholly-within), `crossesImpassable`, `piecesWhollyWithin` (R-3.6 area terrain). Shared geometry (`segmentIntersection2D`, `wallCrosses3D`, `ruinFootprintBlocks` §3.7 both-ends-outside gate, `crateBlocks`, `acrossBarricade` R-3.9) used by `los.ts` too |
| LoS (`src/engine/los.ts`) | `LosService` real: `visible`/`fullyVisible` (model blockers are true elliptical cylinders — the full segment/ellipse intersection interval is checked against the blocker's height range, not just the 2D-closest point, so a ray can graze past an oval base's narrow side or clip a tall blocker's upper cylinder correctly), `unitVisible`/`unitFullyVisible` (attached-leader halves per LEAD-004), `benefitOfCover` (R-3.11–3.14, per-piece attribution via `pieceObstructs` so cover is never granted by unrelated terrain elsewhere), `plungingFire` (R-3.8) |
| Hooks (`src/engine/hooks-impl.ts`, `src/engine/code-hooks.ts`) | `HookService.collect/run/apply` dispatch every active datasheet/leader/enhancement ability + `ActiveEffect` at a hook point, scope-aware (bearer/self/attacker/target/friendly-within/enemy-within); `code-hooks.ts` implements all 29 named code hooks from data (ability, stratagem legality/effect, mission placeholders) plus the reaction-request queue (`pushReaction`/`pendingReactions`/`consumeReaction`) phase modules will read for Fire Overwatch etc. |
| Effects (`src/engine/effects.ts`) | `EffectService.grant/expire/activeFor` — duration-scoped grants (`untilEndOf*`, `nextOwnTurn`) expired at phase/turn/round boundaries and turn start |
| Stratagems (`src/engine/stratagems.ts`) | `stratagemWindow`/`reactionWindow`/`commandReroll` decisions fully handled: affordability + legality (targets, once-per-phase/turn/battle limits, R-1.6 no re-roll of an already-rerolled die), CP spend, effect application via hooks/effects, `StratagemUsed`/`StratagemWindowClosed` events; window offer order (reaction before stratagem, repeat while something is usable) |
| Leaders (`src/engine/leaders.ts`) | Attach/detach bookkeeping, `combinedModels`/`halves` for coherency, targeting and LoS/cover treating an attached pair as one unit |
| Enhancements (`src/engine/enhancements.ts`) | Bearer-scoped ability lookup for the warlord's enhancement, including Tellyporta-style `choice` pairs |
| Tests | `tests/engine/{terrain?,los,los.verify,hooks,hooks.strat,hooks.verify}.test.ts` — 282 tests total, all green (`npm test`); `npm run typecheck` clean |
| Fixed during verification | `los.ts` model-blocker test replaced (2D-closest-point + bounding-circle radius → real ellipse/segment interval vs. height-range overlap: LOS-004-slope, LOS-004-oval); `terrain.ts` `canEndAt` barricade/solid-overlap checks replaced (`pointInPolygon` on the base centre only → `partlyWithinPolygon` so an overhanging base is caught; barricade check no longer skipped at ground level; new wall-panel occupancy check so a model can't end a move inside a ruin wall it's otherwise allowed to cross: LOS-030-end, LOS-022-inside-overlap, LOS-010-wall-end). `pointToSegment` exported from `geometry.ts` for the new wall check |

## Engine (M1 systems — W1-C command/movement/transports + W1-D attack/weapons + W1-F setup deployment/objectives/missions, commit "M1: systems")

| Area | State |
|---|---|
| Command (`src/engine/phases/command.ts`) | Real: CP gain, Battle-shock tests/recovery, `command.start`/`command.battleShock`/`command.end` windows |
| Movement (`src/engine/phases/movement.ts`) | Real: `chooseUnitToActivate` → `declareMove` → `moveUnit` per unit; Normal/Advance/Fall Back/Stationary, Desperate Escape (R-4.6b), coherency cull inputs, Reserves arrivals (Deep Strike/Rapid Ingress) via `deployUnit` at `movement.reinforcements`, end-of-round-3 stranded-Reserves destruction (R-5.14) that now also destroys embarked passengers rather than skipping them, `movement.moveStarted`/`unitMoved` windows (Fire Overwatch), Desperate-Escape surge move helper (`resolveSurgeMove`, exported for the Shooting phase's `shockTactics`/`pistonDrivenBrutality` hooks) with a path-crosses-enemy-model check matching normal moves |
| Transports (`src/engine/transports.ts`) | Real: `embark`/`disembark`/`canEmbark` (capacity, 3" proximity, friendly-only, not this-phase-disembarked, not this-phase-arrived-from-Reserves, not Remained Stationary), `canDisembarkAt`, destroyed-transport passenger handling |
| Attack sequence (`src/engine/attack.ts`) | Real shared sequence used by (future) shooting/charge/fight: hit→wound→allocate→save→damage per model/weapon/target group, Sustained/Lethal/Devastating Wounds (deferred criticals resolve once every group *against that target* is done, before any attack on a different target — R-6.6), Anti-X, re-roll offers (R-6.24), Feel No Pain, Deadly Demise (R-10.4, includes the exploding model's own unit), Hazardous, mortal-wound queue (`queueMortalWounds`) shared with Tank Shock/Grenade/Deadly Demise |
| Weapons (`src/engine/weapons.ts`) | `effectiveWeapon`/ability-value helpers the attack sequence and movement (Heavy stationary bonus) read |
| Setup/deployment (`src/engine/setup.ts`) | Real: P6 alternating `deployUnit` (Defender first, CP-1.10), Reserves/Deep Strike opt-out, attached Leader+Bodyguard as one drop, P7 first-turn (fixed/roll-off/attacker's `chooseOption`), P8 Scouts moves alternating one unit at a time from the first-turn player |
| Objectives/missions (`src/engine/objectives.ts`, `src/engine/missions.ts`) | Real: `evaluateControl` (secure/break CP-2.4/2.5, sticky CP-2.6, claims), scoring-moment live control (`liveController`/`holds`/`controls` all agree: secured-but-just-out-LoC'd breaks at a Command-phase-end evaluation same as `evaluateControl`; 0-vs-0 with nobody ever having been near the marker still trusts the stored controller for isolated scoring-math setups, but not once anything in the battle has actually been destroyed — R-12.3), all 29 named mission code hooks, CP economy, game end/`finalResult` |
| Tests | `tests/engine/{command,movement,movement.verify,attack,attack.verify,missions,missions.verify}.test.ts` — 558 tests total, all green (`npm test`); `npm run typecheck` clean |
| Fixed during verification | `attack.ts`: Deadly Demise wrongly excluded the exploding model's own unit from "every unit within 6\""; Devastating Wounds criticals were deferred past *every* group in the sequence (any target) instead of just the rest of their own target's groups, so a normal attack on target 2 could resolve before target 1's mortal wounds. `missions.ts`: `liveController` trusted a stale `securedBy`/`controller` even when it had just been broken this same evaluation, and non-holdObjectives rules (`properLootin` etc.) read the raw stale `controller` field instead of the live view. `setup.ts`: Scouts alternation ran all of one player's units before the other's instead of alternating one at a time. `transports.ts`: `canEmbark` didn't check "arrived from Reserves this phase" or "Remained Stationary". `movement.ts`: stranded-Reserves culling skipped embarked passengers entirely (leaving them stuck in Reserves forever, or orphaned if the transport died some other way); the `movement.unitMoved` window's re-entry threw `EngineInvariantError` because the per-unit-move marks it needs were already cleared before the window opened; `resolveSurgeMove` didn't check for an intervening enemy model blocking the path. |

## Not yet built

- Phase modules `src/engine/phases/{shooting,charge,fight}.ts`, `tools/sim` — still stubs per `src/engine/phases/README.md` ownership table (W1-D/E/G); they build on `attack.ts`/`weapons.ts` and the M1-foundations services, all landed above
- AI (`src/ai`), figure assets (`src/assets`) — placeholders only (Combat Patrol data is done, see Data)

## Notes for the next agent

- `npm run typecheck` / `npm test` / `npm run build` all pass as of M0 (see verification log at commit time).
- `npm run e2e` requires `npx playwright install chromium` once per machine/CI runner.
- Engine contracts are frozen: change `src/engine/types.ts`/`actions.ts`/`events.ts`/`hooks.ts` only with a matching edit to `docs/spec/00-architecture.md`.
- Movement is two decisions per unit (`declareMove` → `moveUnit`); Fire Overwatch windows are `movement.moveStarted`/`unitMoved`/`reinforcements` and `charge.moveStarted`/`moveEnded` (10-rules R-5.1, R-8.3, R-11.5). Optional re-rolls follow R-6.24 (`rerollOffer`).
- Data shape types live in `src/data/types.ts`; runtime (resolved) shapes in `src/engine/types.ts` (`Runtime*`). Pre-game choices (patrol, enhancement + `enhancementChoice` unit ref, secondary, leader attachments, reserves) are part of `GameSetup`, not decisions; side/first-turn roll-offs surface as `chooseOption` decisions.
- `createGame` now genuinely stops at the first `deployUnit` decision (real deployment, W1-F) — any test/fixture that used to assume a stub deployment completed instantly for free must drive real `deployUnit`/`moveUnit`(Scouts)/`chooseOption` decisions to get past it first; see `tests/fixtures/modules.ts`'s `deployAll` helper (drives every decision whose `window` is `'deployment.unit'` using its own default legal action) and its many call sites in `tests/engine/core*.test.ts`.
- Next up is W1-D/E: the Shooting/Charge/Fight phase modules are still stubs and can now build on the real shared attack sequence (`attack.ts`), weapon helpers (`weapons.ts`), and every M1-foundations service (terrain/los/hooks/effects/stratagems/leaders/enhancements) plus the M1-systems ones (movement's `resolveSurgeMove`, transports, objectives/missions live control) — see `src/engine/phases/README.md` §4. `services.stratagems.openWindow`/`ctx.window` and `code-hooks.ts`'s `pendingReactions`/`consumeReaction` queue are ready for Fire Overwatch / Heroic Intervention / Rapid Ingress / Counter-offensive once a phase module calls them.
- `terrain.canEndAt`, `los.ts`'s model-blocker test, and now several M1-systems modules (see "Fixed during verification" above) were adversarially wrong on first pass — worth a similar adversarial pass on Shooting/Charge/Fight before trusting them past their own happy-path tests.
